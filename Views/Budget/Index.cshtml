@model IQueryable<Budget>

<div>
    <h1>Budget List</h1>
</div>

<ul>
    @foreach (Budget b in Model)
    {
        <li>
            @b.Description ||| @b.StartDate.ToString("MMM dd yyyy") ||| @b.EndDate.ToString("MMM dd yyyy")
        </li>
    }
</ul>

